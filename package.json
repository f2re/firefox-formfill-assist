{
  "name": "firefox-formfill-assist",
  "version": "0.4.1",
  "private": true,
  "type": "module",
  "description": "Safety-first Firefox extension for screenshot-to-AI form filling with strict JSON, preview and no auto-submit.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "package": "node scripts/package.mjs",
    "verify:version": "node scripts/check-version.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "check": "npm run verify:version && npm run typecheck && npm run test",
    "lint:extension": "web-ext lint --source-dir dist",
    "dev": "node scripts/build.mjs --watch"
  },
  "dependencies": {
    "preact": "^10.27.1",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/firefox-webext-browser": "^120.0.4",
    "@types/node": "^24.0.0",
    "jsdom": "^26.1.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vitest": "^3.2.4",
    "web-ext": "^8.9.0"
  },
  "engines": {
    "node": ">=22"
  }
}
